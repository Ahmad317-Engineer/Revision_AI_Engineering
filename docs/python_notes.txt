Python is a programming language created by Guido van Rossum in 1991. It is easy to read and write.

pip is the tool used to install Python packages. For example: pip install openai

A virtual environment keeps your project packages separate from other projects. Use python -m venv myenv to create one.

Functions in Python are defined using the def keyword. They take input, do something, and return output.